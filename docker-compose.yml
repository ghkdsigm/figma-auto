services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: company
      POSTGRES_PASSWORD: company_pw
      POSTGRES_DB: a2ui_codegen
    ports: ["15432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U company -d a2ui_codegen"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7-alpine
    ports: ["16379:6379"]

  toolserver:
    build: { context: ./toolserver }
    environment:
      FIGMA_TOKEN: ${FIGMA_TOKEN}
      FIGMA_API_BASE: ${FIGMA_API_BASE:-https://api.figma.com}
      PORT: 4010
    ports: ["4010:4010"]

  api:
    build: { context: ./backend }
    env_file: [./.env]
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_started }
      toolserver: { condition: service_started }
    # Windows can reserve/exclude port 3000 (see `netsh int ipv4 show excludedportrange protocol=tcp`),
    # which prevents Docker from binding. Use a non-excluded host port by default.
    ports: ["${API_PORT:-3100}:3000"]
    volumes: ["artifacts:/data/artifacts"]

  web:
    build: { context: ./web }
    environment:
      # Must match the host-side API port mapping above.
      NUXT_PUBLIC_API_BASE: "http://localhost:${API_PORT:-3100}"
      PORT: 3001
      HOST: 0.0.0.0
      NITRO_PORT: 3001
      NITRO_HOST: 0.0.0.0
    depends_on: [api]
    ports: ["${WEB_PORT:-3101}:3001"]


volumes:
  pgdata:
  artifacts:
